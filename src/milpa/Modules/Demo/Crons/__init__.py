"""Crons AGENDADOS del demo (`@cron_task(schedule=...)`). Se auto-registran (`import_all_tasks`
escanea Crons/) y los despacha `schedule run`. Distintos de los jobs (Jobs/): los AGENDA el
scheduler, no los disparas tú."""
