// src/modules/reportes/pages/index.jsx → /reportes
// Módulo SIN _layout.jsx: sus páginas se montan directo (el layout es opcional).
export default function ReportesHome() {
    return (
        <article>
            <h2>Reportes</h2>
            <p>Módulo de OTRO equipo: carpeta propia en <code>src/modules/reportes/</code>,
               sin layout propio (opcional), montado solo en <code>/reportes</code>.</p>
        </article>
    );
}
