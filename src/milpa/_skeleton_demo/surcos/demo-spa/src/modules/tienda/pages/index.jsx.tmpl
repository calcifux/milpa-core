// src/modules/tienda/pages/index.jsx → /tienda  (cero registro manual)
export default function TiendaHome() {
    return (
        <article>
            <h2>Tienda</h2>
            <p>Esta página existe porque existe el archivo
                <code> modules/tienda/pages/index.jsx</code> — nadie tocó un router central.</p>
        </article>
    );
}
