export default function Acerca() {
    return (
        <article>
            <h2>Acerca</h2>
            <p>Forma tradicional: el frontend vive en otro servidor con CORS.</p>
            <p>Estilo milpa: el backend es dueño del shell e inyecta los assets
               de Vite desde el manifest — un solo origen, cero CORS.</p>
        </article>
    );
}
