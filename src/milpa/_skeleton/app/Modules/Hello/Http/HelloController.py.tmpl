"""Módulo de ejemplo 'Hello' — generado por `milpa new`.

milpa descubre este controller SOLO: escanea `app/Modules/*/Http/` y monta cualquier
`@Controller`. Estilo class-based (parecido a Spring): la clase agrupa las rutas del módulo
y `self` te da estado por-request si lo necesitas. No hay que registrarlo en ningún lado.
Copia esta carpeta para tu primer módulo real (o usa `jornal make module <Nombre>`), o bórrala.
"""

from __future__ import annotations

from fastapi.responses import HTMLResponse

from milpa.Core.Http import Controller, Get
from milpa.Core.View import view


@Controller("", tags=["hello"])
class HelloController:
    @Get("/hello")
    def hello(self) -> dict[str, str]:
        """Ruta JSON de cortesía: confirma que milpa descubrió tu módulo."""
        return {"mensaje": "¡milpa está sembrada! 🌽", "modulo": "Hello"}

    @Get("/")
    def welcome(self) -> HTMLResponse:
        """Página de bienvenida: renderiza app/Resources/Views/welcome.html.j2."""
        return view("welcome", {"titulo": "__PROJECT__"})
