#!/usr/bin/env python3
"""jornal — la consola de TU proyecto (el "artisan" local de __PROJECT__).

Es un lanzador fino: el CLI de verdad (descubrimiento de comandos + Typer) vive en
milpa. Aquí solo se arranca, para tenerlo a la mano en la raíz del proyecto.

Uso:
    python jornal list           # ver todos los comandos
    python jornal serve          # levantar la API (http://127.0.0.1:8000)
    python jornal make:module X  # generar un módulo nuevo
    ./jornal serve               # si el venv está activo y el archivo es ejecutable
"""

from milpa.Core.Console.Cli import app

if __name__ == "__main__":
    app()
